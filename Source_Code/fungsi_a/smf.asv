function[f,g] = smf(x)
    [n,k] = size(x);
    f = 0;
    g = zeros(n);
    newPi = 3.141592653589793
    
    for i = 1:n
        value = i * x(i) * x(i) / newPi;
        f = f - sin(x(i)) * sin(value);
        g(i) = -cos(x(i)) * 
    end
    
end